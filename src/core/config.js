/* Site settings you may want to change.

   PRESENCE_URL — the endpoint that counts who is online. Leave it empty and
   the counter simply does not appear, which is the right setting for a plain
   GitHub Pages deploy. To switch it on, deploy tools/presence-worker.js and
   paste its URL here. */
export const PRESENCE_URL = '';
